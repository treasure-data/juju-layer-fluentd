repo: https://github.com/SaMnCo/layer-fluentd.git
includes: ['layer:basic', 'interface:elasticsearch'] 
options:
  basic:
    packages:
      - git
      - build-essential
      - software-properties-common
      - curl
      - wget
      - bundler